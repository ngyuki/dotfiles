#!/bin/bash

set -eu

export GIT_SHA="$(git show --format=format:%h --no-patch "$1")"
export GIT_SEQUENCE_EDITOR="sed -i -r '/^p(ick)? $GIT_SHA/s/^p(ick)? /reword /'"
git rebase --interactive --rebase-merges --no-autosquash --autostash "${GIT_SHA}^"
